apiVersion: 1

providers:
  - name: "LeLRay"
    orgId: 1
    folder: "LeLRay"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10

    options:
      path: /var/lib/grafana/dashboards